generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "cockroachdb"
  url      = env("DATABASE_URL")
}

model skins {
  id            BigInt  @id
  src           String?
  correctanswer String?
  rarity        String?
}
